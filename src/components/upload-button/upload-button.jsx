import React from 'react';
import { Button } from '@mui/material';

const UploadButton = () => {
  const handleClick = () => {};

  return (
    <Button className='upload-button' variant='contained' onClick={handleClick}>
      Upload
    </Button>
  );
};

export default UploadButton;
